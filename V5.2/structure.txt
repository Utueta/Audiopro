Audio_Expert_Pro/
├── app.py                 # Orchestrateur (Contrôleur) : Gère le multi-threading et les flux de données.
├── analyzer.py            # Moteur Physique : DSP, FFT, détection Fake HQ, Hash Blake2b et Mutagen.
├── model.py               # Cerveau ML : Random Forest, persistance SQLite et auto-ajustement des poids.
├── llm_service.py         # Arbitre IA : Connexion Ollama/Qwen 2.5 pour la "zone grise".
├── view.py                # Interface (GUI) : Affichage PySide6, Spectrogrammes et monitoring GPU.
├── config.json            # Centre de commande : Seuils, poids ML, extensions et réglages LLM.
├── requirements.txt       # Dépendances : Liste des librairies Python (librosa, PySide6, sklearn, etc.).
│
├── scripts/               # Utilitaires de maintenance
│   ├── install_deps.sh    # Script d'installation des dépendances système (FFmpeg, NVML).
│   ├── reset_brain.sh     # Script de purge (DB + Modèle ML) pour repartir à zéro.
│   └── check_health.py    # Diagnostic de santé (Vérification NVIDIA + Ollama + Audio).
│
├── database/              # Stockage persistant
│   └── audio_expert_v01.db # Base SQLite (générée au premier lancement).
│
├── models/                # Modèles entraînés
│   └── audio_expert_rf.joblib # Modèle Random Forest (généré/mis à jour via Retrain).
│
└── logs/                  # Rapports et erreurs
    └── session.log        # Historique des erreurs et succès du scan.
